name: hello
on:
    pull_request:
        branches:
            - dev
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - name: hello 
              run: print thanks for pull request