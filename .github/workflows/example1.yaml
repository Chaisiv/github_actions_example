name: print_hello
on:
    push:
        branches:
            - main
jobs:
    print_hello:
      runs-on: ubuntu-latest
      steps:
        - name: hello
          run: "echo hello how are you "            